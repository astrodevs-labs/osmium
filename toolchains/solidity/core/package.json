{
  "name": "osmium-solidity-core",
  "version": "0.0.2",
  "license": "MIT",
  "scripts": {
    "build": "cd crates/extension && make && cd ../..",
    "lint": "cargo clippy -- -D warnings",
    "format": "cargo fmt --check --all",
    "test": "cargo test --workspace",
    "publish": "cd crates/linter-lib && make publish && cd ../.."
  }
}
