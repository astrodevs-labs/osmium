import './InteractButton.css';

export const InteractButton = () => {
  return (
    <div className="button-container">
      <button className="deploy-button">Send Transaction</button>
    </div>
  );
};
