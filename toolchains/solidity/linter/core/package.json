{
  "name": "osmium-solidity-linter-core",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "build": "cargo build --release",
    "lint": "cargo clippy -- -D warnings",
    "format": "cargo fmt",
    "test": "cargo test",
    "publish": "yarn run build && cp target/release/solidhunter ../extension/core"
  }
}
